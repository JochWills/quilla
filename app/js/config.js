// Public configuration for the Quilla app.
// These values are safe to expose in the browser: the anon key only works within
// the row level security policies defined in supabase/migrations.
// Find them in Supabase → Project Settings → API.
export const SUPABASE_URL = "https://YOUR-PROJECT-REF.supabase.co";
export const SUPABASE_ANON_KEY = "YOUR-SUPABASE-ANON-KEY";

// Where the marketing site lives (used for the logo link and legal links).
export const SITE_URL = "https://quilla.co.za";
